/**
 * ABeam Workbench — Affirm external legal / consent versioning.
 *
 * The guest landing records which acknowledgement + PDPA notice the executive
 * accepted. Bump these when the copy changes so historical grants pin the
 * version they consented to (stored on AffirmAccessGrant.pdpaVersion).
 */

export const AFFIRM_ACK_VERSION = "affirm-ack-v1";
export const AFFIRM_PDPA_VERSION = "affirm-pdpa-v1";

/** Rough time expectation shown on the landing + invite. */
export const AFFIRM_TIME_EXPECTATION = "about 20 minutes";
